import 'package:flutter/material.dart';

class zonk extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Container(
          color: Colors.blue,
          child: Text('asdbbbabf'),
        ),
        Container(
          color: Colors.orange,
          child: Image.asset('assets/images/logo_full.png'),
        ),
      ],
    );
  }
}
